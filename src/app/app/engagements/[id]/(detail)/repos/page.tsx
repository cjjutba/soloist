import { TabPlaceholder } from "../tab-placeholder";

export default function ReposTab() {
  return (
    <TabPlaceholder
      title="Repo connections"
      body="Connect GitHub repositories to this engagement to auto-pull activity. Arriving in Epic 3."
    />
  );
}
