import { TabPlaceholder } from "../tab-placeholder";

export default function DocumentsTab() {
  return (
    <TabPlaceholder
      title="Documents"
      body="Create and send invoices (Draft → Sent → Paid). Arriving in Epic 5."
    />
  );
}
